# Brand Terms — {Your Brand}

Do Not Translate (DNT) terms for {your product}. Keep every term below verbatim in every translated string, even when the surrounding sentence is translated. Replace this template's placeholders with your own terms, or leave the list empty if your product has none.

## The terms

- `{Brand Name}` — the product/company name.
- `{Feature Name}` / `{Feature Names}` — list singular and plural forms separately if both appear in your strings.
- `{Term with a generic meaning}` — if a brand term is also an ordinary word (e.g. a feature called "Feed"), say when it's the product (keep verbatim, capitalised) and when it's the ordinary word (translate normally). Give an example sentence of each so the distinction is applied by reading, not by pattern-matching.

> If a brand/product term is encountered that is not on this list, stop and ask before translating.

## Known mistranslations (optional)

If machine translation has previously mangled your terms, list the wrong forms so they're never reproduced:

- `{wrong form}` → write `{canonical term}` instead

## Example

Show one string from your product with the brand term kept and the rest translated:

- EN: `'{key}' => ':count new {Feature}|:count new {Features}'`
- DE: `'{key}' => ':count neuer {Feature}|:count neue {Features}'` — placeholders and `|` preserved, ordinary words translated, `{Feature}` kept as-is.
